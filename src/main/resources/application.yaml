

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/market?currentSchema=shop_market
    username: user1
    password: 1111
  liquibase:
    change-log: classpath:db/changelog-master.xml

server:
  port: 8090
